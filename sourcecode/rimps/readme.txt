if you are new to RIMP file formats, here is what they basically are:

A external way of importing needed assembelies for scripts, so instead of doing "import radio.afterlife" you don't have to do anything, they'll be automatically
imported and used in scripts when called.